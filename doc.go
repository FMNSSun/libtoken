// The libtoken package provides an interface and generators
// to generate various sorts of tokens (or random strings) using
// the native crypto/rand cryptographically strong random number
// generator if available. If not available it will use a fall back
// random number generator. 
package libtoken
